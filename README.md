# QR Stock Tracking

Monorepo that hosts the Spring Boot backend and Flutter mobile client for a QR-code powered stock management MVP.

## Getting Started

```bash
mvn -f server/pom.xml clean package
docker compose up --build
```

The backend becomes available on http://localhost:8080 and the PostgreSQL instance on localhost:5432.

Default credentials: admin / admin123.


